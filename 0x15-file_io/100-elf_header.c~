#include "main.h"

/**
 *main - pweuhfpwdnfoiwjfoi
 *argc: qwiejfiodwoicwj
 *argv: wpiefIASKÑWIOEpo
 *Return: return 0
 */
int main(int argc, char *argv[])
{
	if (argc != 2)
	{
		dprintf(STDERR_FILENO, "usage: elf_header elf_filename\n");
			exit(98);
	}
	(void)argv;
	return (0);
}
