#include "holberton.h"
#include <stdio.h>

/**
 * main - print name file new line
 * @argc: argument
 * @argv: arguments main and array of strings
 * Return: 0
 */

int main(int argc, char *argv[])
{
       

	printf("%s\n", argv[0]);

	return (0);
}
