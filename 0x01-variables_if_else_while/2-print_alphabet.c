#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *main - Entry point
 *this fuction print the alphabet a...z, A...Z
 *
 *Return: Always 0 (success)
 */
int main(void)
{
char start = 'z';
char aux;
for (aux = 'a'; aux <= start; aux++)
{
putchar(aux);
}
putchar('\n')
{
putchar(aux);
}
return (0);
}
